# followthemoney-graph

The followthemoney-graph (`ftmg`) tool helps to transform and load [FollowTheMoney](https://www.followthemoney.tech) entity data into a Neo4J property graph database. In the process, the data can be filtered and re-interpreted in various ways, e.g. to reify specific entity properties (such as names or identifiers) into graph nodes.
