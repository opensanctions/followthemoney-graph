
* Use the `followthemoney` Python package whereever appropriate. Docs: https://followthemoney.tech/python/
* Produce clean and type-annotated Python 3.12+ code
* Whenever a configuration option is needed, propose changes to `config.yml` and the corresponding classes in 
* Always check that the transforms work if data is out of sequence, ie. nodes are defined after edges that reference them. 